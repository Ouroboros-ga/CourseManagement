# 查课管理系统 V1.0 开发技术方案

> Python / FastAPI 后端 · 原生微信小程序 · Web 管理后台

| 项目 | 内容 |
|---|---|
| 文档版本 | V1.5（权限策略 V1.1：教师角色仅由超管管理，当前开发基线） |
| 对应产品版本 | V1.0 |
| 编制日期 | 2026-09-20 |
| 产品形态 | 原生微信小程序 + Web 管理后台 |
| 后端架构 | FastAPI + SQLAlchemy + Alembic + MySQL + openpyxl，模块化单体 |
| 小程序 | WXML + JavaScript + WXSS + JSON |
| Web | Vue 3 + TypeScript + Vite + Element Plus |
| 数据库 | MySQL 8.4 / InnoDB |
| 文档性质 | 开发设计基线；参数建议不代表已获业务确认或已完成性能验证 |

## 1. 修订范围与决策状态

本文依据《查课管理系统V1.0建设说明》、原开发技术方案及本轮需求调整重新编制，可独立用于后续需求细化、数据库设计、接口设计与开发排期。

本版按用户最终选型替代 V1.1 后端技术基线：采用用户熟悉的 Python 技术栈，提高开发与维护效率。原生微信小程序、Vue 管理端及已确定的业务规则保持一致。当前仓库的旧初始化说明、database_design.md 和 database.sql 尚未按完整查课模型同步，不能直接当作本版实施与建库依据。

### 1.1 已确定事项

1. 后端采用 FastAPI + SQLAlchemy 2.x + Alembic + MySQL 8.4 + openpyxl，保持模块化单体，数据库采用同步 PyMySQL 驱动。
2. 微信小程序使用原生 WXML、JavaScript、WXSS 和 JSON；不采用 uni-app、Vue 小程序或 Taro。
3. “年级负责人”统一改为“学生工作负责人”，其数据范围覆盖本系统所有年级。
4. 统计查看、异议初核等可选权限默认关闭，由教师管理员逐人打开或关闭。
5. 查课照片与异议证明材料分别定义保留期限。
6. 附件采用私有存储，申请短时访问链接时鉴权；不建设每次下载重新鉴权或已签发下载链接即时撤销机制。
7. 管理端支持配置本日查课数据提交截止时间。
8. 周报保留历史版本，展示最新版本及其更新时间。
9. 按第 5 节目录组织工程，导入、排班和周报采用有界同步执行；到期清理由部署定时脚本负责，V1.0 不建设独立 worker.py、job/ 或通用任务队列。
10. 权限按 docs/PERMISSIONS.md V1.1 执行：38 个 Permission，教师仅可手工分配负责人角色，教师角色仅由超管管理；学生工作负责人默认具备 identity.binding.manage。
11. STUDENT 由有效学生绑定自动维护，VOLUNTEER 由学期资格驱动；未绑定微信用户使用 PRE_BINDING 受限会话，不新增角色。
12. 排班基本信息随 inspection.read 读取；志愿者改派、资格停用或学期结束后，保留本人历史参与及本人照片的受限只读权。

### 1.2 本版推荐规则与待定参数

以下是可实现的推荐基线，尚未被用户逐项确认；应在相应功能开发前冻结，不能当作既有业务制度。

| 项目 | 本版建议 |
|---|---|
| 截止后处理 | 允许补交并标记逾期；不直接关闭提交入口 |
| 默认每日截止时间 | 示例 22:00，实际值由管理端配置 |
| 查课照片期限 | 示例：学期结束后 12 个月，待学院确定 |
| 异议材料期限 | 示例：终审后 12 个月，待学院确定 |
| 未关联临时文件期限 | 示例：24 小时，待确定 |
| 已通过审核的任务取消 | 禁止直接取消；V1.0 通过更正处理错误 |
| 周报“最终版本” | 解释为当前最新版本，允许以后生成修订版本 |
| 基础快照时点 | 任务生成时冻结名单，执行前需更正时走带审计的名单修订 |

## 2. 建设目标与业务范围

系统覆盖以下闭环：

```text
学期、学生、行政班、教学班与课表
→ 查课任务生成
→ 自动排班与人工调整
→ 志愿者录入事实并上传材料
→ 查课审核
→ 当前有效考勤与历史版本
→ 学生查询与异议
→ 初核、终审与考勤更正
→ 统计与版本化周报
```

V1.0 包含早自习、正常课程、晚自习；行政班与教学班分离。同一个微信小程序承载学生端和志愿者端，身份可重叠。

不建设微服务、分布式数据库、复杂消息平台、人脸识别、定位打卡、自动纪律处分、周报发布审批流程。Redis、MQ 和集群仅在实际需求与测量结果证明必要时引入。

## 3. 总体架构

```text
Web 管理后台                         原生微信小程序
Vue 3 / TypeScript                   WXML / JS / WXSS / JSON
       │ HTTPS                              │ HTTPS
       └────────────────┬───────────────────┘
                     Nginx
                       │
              Uvicorn + FastAPI + Pydantic
                       │
     身份权限 / 基础数据 / 任务排班 / 考勤异议
              文件 / 报表 / 审计
                       │
              SQLAlchemy 2.x + PyMySQL
                       │
                  MySQL 8.4

附件与周报文件 → 私有 COS / OSS
部署定时器 → deploy/scripts/ 清理入口 → 文件服务与 MySQL
```

V1.0 的 Excel 导入、自动排班和周报生成在请求内同步完成，限制输入规模、执行时间和并发。材料清理由部署环境定时调用独立脚本，复用文件模块的服务逻辑。保留业务批次、版本和幂等记录，不引入通用任务调度与租约模型。

## 4. 技术栈与选型理由

| 层次 | 技术 | 用途与边界 |
|---|---|---|
| Python | Python 3.12 基线 | 锁定受支持的补丁版本，开发、CI 与生产保持一致；更高版本经兼容验证后采用 |
| 应用框架 | FastAPI + Uvicorn | REST API、依赖注入、异常处理与 ASGI 服务 |
| 数据校验 | Pydantic 2.x + pydantic-settings | 请求/响应模型、环境配置；拒绝客户端写入服务端所有权字段 |
| 安全 | FastAPI Dependencies + 集中权限服务 + 成熟令牌/密码库 | 统一身份与功能校验，业务服务强制资源归属；框架不自动提供完整权限模型 |
| 数据访问 | SQLAlchemy 2.x ORM/Core + PyMySQL | 同步 Session，显式范围过滤、条件更新与锁定查询 |
| 连接池 | SQLAlchemy QueuePool | 限制 pool_size、max_overflow、pool_timeout，配置连接检测与回收 |
| 迁移 | Alembic | 版本化迁移；人工审核自动生成结果，不用 create_all 修改生产表 |
| Excel | openpyxl | 模板使用普通模式；大文件按场景使用 read_only / write_only |
| API 契约 | FastAPI 生成 OpenAPI 3 | 明确响应模型、权限、错误码与幂等语义，CI 检查接口变更 |
| 测试 | pytest + HTTPX/TestClient + 独立 MySQL 测试库 | 真实 MySQL 约束、事务与竞争场景验证；可用 Docker/Testcontainers 管理测试实例 |
| 工程质量 | pyproject.toml + 依赖锁文件 + Ruff + mypy | 固定依赖、格式检查、关键业务类型检查；禁止动态 latest |
| Web | Vue 3、TypeScript、Vite、Vue Router、Pinia、Element Plus | 管理界面 |
| 小程序 | WXML、JavaScript、WXSS、JSON、微信开发者工具 | 微信原生页面、组件、登录、上传与下载 |
| 部署 | Docker Compose、Nginx、HTTPS | 单机起步，可迁移托管数据库 |

本次选择基于用户对 Python 栈的熟悉程度，以及接口、数据处理和 Excel 开发效率。语言选择不构成性能承诺，数据库慢查询、连接池等待、锁竞争和文件带宽仍需测量。

V1.0 使用同步 SQLAlchemy + PyMySQL。访问同步数据库的路由采用普通 def，由框架线程池调度；在 async def 中直接调用普通同步函数不会自动转入线程池。确需异步路由时，将完整同步业务单元移交线程池，并在该单元内部管理 Session，不能让同步查询阻塞事件循环。

业务正确性优先于“全部 async”。只有压测证明 I/O 并发成为瓶颈，再评估异步驱动及 AsyncSession；变更必须同时审查调用链、事务与测试。

## 5. 工程结构与代码约定

```text
CourseManagement/
├── apps/
│   ├── admin-web/
│   │   └── src/
│   │       ├── api/
│   │       ├── views/
│   │       ├── components/
│   │       ├── stores/
│   │       ├── router/
│   │       └── utils/
│   └── wechat-miniapp/
│       ├── app.js
│       ├── app.json
│       ├── app.wxss
│       ├── pages/
│       ├── components/
│       ├── services/
│       ├── utils/
│       └── project.config.json
├── backend/
│   ├── pyproject.toml
│   ├── uv.lock
│   ├── alembic.ini
│   ├── Dockerfile
│   ├── app/
│   │   ├── main.py
│   │   ├── core/
│   │   │   ├── config.py
│   │   │   ├── database.py
│   │   │   ├── security.py
│   │   │   ├── permissions.py
│   │   │   ├── exceptions.py
│   │   │   ├── middleware.py
│   │   │   └── logging.py
│   │   ├── common/
│   │   │   ├── pagination.py
│   │   │   ├── responses.py
│   │   │   └── validators.py
│   │   └── modules/
│   │       ├── identity/
│   │       ├── academic/
│   │       ├── inspection/
│   │       ├── attendance/
│   │       ├── objection/
│   │       ├── report/
│   │       ├── file/
│   │       └── audit/
│   ├── migrations/
│   │   ├── env.py
│   │   └── versions/
│   └── tests/
│       ├── unit/
│       └── integration/
├── deploy/
│   ├── docker-compose.yml
│   ├── nginx/
│   └── scripts/
├── docs/
│   ├── PRD.md
│   ├── TECHNICAL_DESIGN.md
│   ├── DATABASE.md
│   ├── API.md
│   ├── PERMISSIONS.md
│   └── templates/
└── README.md
```

以上为用户确认的目标目录；尚未创建的文件按实施进度补齐，不将目录清单视为已完成工程。Python 包初始化文件和前端构建配置在搭建时补充。并发测试归入 tests/integration/，不另设 concurrency/。

core/ 只放应用配置、连接、安全基础和统一异常/中间件；common/ 放跨业务的分页、响应与通用校验。具体资源归属规则留在模块 permissions.py，避免把所有业务判断集中到 core/permissions.py。main.py 负责应用组装与路由注册，不放业务流程。

deploy/scripts/ 承载部署与维护入口，包括到期材料清理脚本；脚本调用 file 模块服务，不重复实现删除规则。docs/TECHNICAL_DESIGN.md 是后续整理的技术文档位置，现阶段以本文件为唯一技术基线；迁入后更新入口，避免同时维护两份不一致正文。

业务模块按需划分 router.py、schemas.py、service.py、repository.py、models.py、permissions.py。Router 处理 HTTP，Pydantic Schema 定义输入输出，Service 负责授权、规则和事务，Repository 执行查询，Model 定义表映射。禁止 Router 拼接复杂 SQL，禁止不经响应模型过滤就返回 ORM 实体。

### 5.1 Session 与事务边界

sessionmaker/Engine 由应用配置统一创建，每个 API 业务单元或清理脚本执行单元独立创建 Session。Session 不得跨请求或并发线程共享；子任务只传用户 ID、资源 ID 等普通数据，并自行创建 Session。

Service 顶层使用 sessionmaker.begin() 上下文统一创建 Session、开启事务、提交/回滚及关闭。参与同一业务动作的 Repository 复用这个 Session，不自行 commit；需要主键时使用 flush。commit 必须在成功响应前完成，提交失败转换为规定错误，不在返回响应后的清理阶段才提交。

认证依赖用短只读 Session 得到必要身份信息并关闭；写业务事务内再检查敏感权限、资源与当前状态。不要对已被依赖查询自动开启事务的 Session 再无条件调用 begin。锁定读取可使用 select(...).with_for_update()；版本更新检查带 current_version 条件的 UPDATE 影响行数。

数据库事务中不得等待对象存储、微信接口或 Excel 渲染完成。事务回滚后再进行有限重试；IntegrityError 的已知唯一冲突转换为业务结果，未知数据库错误记录脱敏日志并按服务器错误处理。

### 5.2 模型与迁移

采用 SQLAlchemy 2.x DeclarativeBase、Mapped 和 mapped_column 风格，集中约束命名，供 Alembic 使用统一 metadata。枚举以字符串值存储，避免默认映射成 MySQL 原生 ENUM。序列化前在 Session 范围内取得所需字段，避免关闭 Session 后触发懒加载及列表 N+1 查询。

Alembic revision --autogenerate 只生成候选迁移，必须人工检查重命名、数据回填、索引和约束。CI 验证空库升级、上一版本升级和唯一 migration head。生产发布由单独步骤执行 alembic upgrade head，不由每个 Uvicorn 服务进程 启动时抢跑迁移。

依赖建议通过 uv 管理 pyproject.toml 与 uv.lock，CI/镜像使用锁定安装。开发环境可使用项目 .venv；不依赖全局 Python 的偶然安装状态。

## 6. 角色、功能与数据权限

### 6.1 固定角色

| 角色代码 | 名称 | 数据范围 |
|---|---|---|
| SUPER_ADMIN | 超级管理员 | 本系统全部 |
| TEACHER_ADMIN | 教师管理员 | 本系统全部年级的授权业务 |
| STUDENT_AFFAIRS_MANAGER | 学生工作负责人 | 本系统全部年级，功能受限 |
| VOLUNTEER | 查课志愿者 | 本人负责的任务及任务名单 |
| STUDENT | 普通学生 | 本人考勤与本人异议 |

删除按年级为学生工作负责人授权的设计，不建立该角色的 user_grade_scope。所有年级不代表其他学校或其他机构的数据范围。

管理查询范围使用 SYSTEM_WIDE（当前系统、当前学院）和 ALL_GRADES（当前学院全年级）。教师默认具有 role.assign，手工分配允许列表仅为 STUDENT_AFFAIRS_MANAGER；不得授予或撤销 SUPER_ADMIN、TEACHER_ADMIN（包括自己的教师角色）。只有超管可管理超管和教师角色。完整角色集合更新必须检查增删差集，防止通过省略受保护角色实现撤销。STUDENT/VOLUNTEER 由业务状态维护，不通过普通角色接口绕过绑定与资格。

### 6.2 权限矩阵

| 功能 | 超级管理员 | 教师管理员 | 学生工作负责人 | 志愿者 | 学生 |
|---|---|---|---|---|---|
| 全局账号读取与维护 | 是 | 否 | 否 | 否 | 否 |
| 用户角色分配 | 是，含超管及教师 | 当前学院内仅手工分配负责人角色 | 否 | 否 | 否 |
| 配置负责人可选权限 | 是 | 是 | 否 | 否 | 否 |
| 绑定码签发/作废、核验换绑 | 是 | 是 | 是 | 否 | 否 |
| 学期、学生、班级、课表维护 | 是 | 是 | 否 | 否 | 否 |
| 学生名单查看 | 是 | 是 | 全年级 | 本人任务名单 | 本人信息 |
| 任务和排班查看 | 是 | 是 | 全年级 | 本人任务 | 否 |
| 任务生成、取消与排班修改 | 是 | 是 | 否 | 否 | 否 |
| 查课提交、调班申请 | 按志愿者身份 | 按志愿者身份 | 按志愿者身份 | 本人任务 | 否 |
| 本人历史提交与照片只读 | 按历史参与身份 | 按历史参与身份 | 按历史参与身份 | 本人历史参与 | 按历史参与身份 |
| 查课审核、应到人数修改 | 是 | 是 | 否 | 否 | 否 |
| 考勤查询 | 是 | 是 | 全年级 | 按学生身份 | 本人 |
| 考勤更正、异议终审 | 是 | 是 | 否 | 否 | 否 |
| 异议初核 | 是 | 是 | 可选，默认关闭 | 否 | 否 |
| 统计查看 | 是 | 是 | 可选，默认关闭 | 否 | 否 |
| 周报查看与下载 | 是 | 是 | 可选，默认关闭 | 否 | 否 |
| 周报新版本生成 | 是 | 是 | 否 | 否 | 否 |
| 提交异议 | 按学生身份 | 按学生身份 | 按学生身份 | 按学生身份 | 本人 |
| 截止时间完整配置及历史读取 | 是 | 是 | 是 | 仅本人任务摘要 | 否 |
| 提交截止时间修改 | 是 | 是 | 否 | 否 | 否 |
| 系统审计查看 | 是 | 否 | 否 | 否 | 否 |

### 6.3 可选权限实现

学生工作负责人的角色默认权限不包含三个可选项；超级管理员、教师管理员仍按管理职能具有这些功能的默认权限。对负责人，user_permission 中存在适用的有效授权即开启，移除授权即关闭。教师管理员只能操作许可清单内的权限，不能借此授予终审、角色管理等其他权力。

个人可配置 code 固定为 statistics.read、objection.initial_review、report.read。周报下载包含个人明细，不能由 statistics.read 隐式取得。负责人拥有初核权限时，条件派生全学院全部异议和历史读取路径；关闭后失去该管理路径，不影响仍合法的本人学生读取路径。负责人角色撤销时个人开关失效，重新授予角色不自动恢复旧开关。

每个请求读取有效账号、会话和权限；V1.0 不使用长期权限缓存，关闭权限后下一次 API 请求即生效。已经签发的文件访问链接按其原过期时间失效。

所有列表、搜索、详情、导出、统计、附件和批量写入统一授权。学生 ID、操作者 ID、上传者 ID 由服务端会话取得，不相信客户端传入的身份。多角色合并不应取消“本人任务”等资源限制。

完整资源、38 个 Permission、默认角色矩阵、身份生命周期与 API/查询规则以 [docs/PERMISSIONS.md V1.1](./docs/PERMISSIONS.md) 为权限设计基线；项目接入及静态代码差距见 [PERMISSIONS_IMPLEMENTATION.md](./docs/PERMISSIONS_IMPLEMENTATION.md)，开发顺序见 [开发路线](./docs/DEVELOPMENT_PLAN.md)。设计已采纳不代表运行时已赋权或实现。

## 7. 身份认证与会话

### 7.1 Web 登录

账号密码登录，密码使用 Argon2id 或经验证的 bcrypt 实现；固定参数并压测密码校验成本。登录失败采用账号与来源相结合的限流，避免校园共用出口 IP 造成全校误封。建议超级管理员启用多因素认证。

Web 同源部署。短期访问令牌保存在内存，刷新凭证使用 HttpOnly、Secure、适当 SameSite 的 Cookie；刷新、退出等使用 Cookie 的写接口落实 CSRF 防护及来源校验。

### 7.2 微信登录与绑定

```text
wx.login 获取 code
→ 后端调用微信登录凭证校验接口
→ 获得 openid，并映射本系统 user
→ 未绑定时签发 PRE_BINDING 受限会话
→ 使用受限会话输入学号和一次性绑定码
→ 服务端核验有效学生，原子完成绑定、码核销和 STUDENT 自动维护
→ 后续请求重新解析身份，进入正常学生业务
```

AppSecret 和微信 session_key 只保存在后端，不下发客户端、不写入日志。若未来有多个小程序，身份唯一键使用 appid + openid。

绑定码使用安全随机数生成，只保存不可逆摘要，具有有效期、使用次数与失败次数限制。发放渠道必须能可靠对应本人，不通过公开名单暴露。核销绑定码与写入学生绑定在同一事务完成，数据库唯一约束阻止重复绑定。

超管、教师管理员、学生工作负责人默认具有 identity.binding.manage，可签发/作废绑定码，并在核验后换绑、记录审计和撤销旧会话；此权限不包含全局账号维护或密码重置。学校统一身份认证可后续替换绑定入口。

PRE_BINDING 不是第六角色，仅放行 GET /me、POST /me/student-binding、POST /auth/refresh、POST /auth/logout。刷新不得提升会话权限；所有其他业务入口统一拒绝。该限制针对未绑定微信会话，不因 Web 管理员没有 student_id 而将其误判为受限会话。

### 7.3 会话规则

auth_session 为必需表，保存 user_id、client_type、refresh_token_hash、expires_at、revoked_at 和刷新族信息。访问令牌包含会话引用，接口校验会话有效性；不能仅因 JWT 签名有效就接受已封禁用户。

刷新凭证轮换与旧凭证消费使用原子更新；检测旧凭证异常复用时撤销对应刷新族。客户端串行刷新，多请求等待同一次刷新完成，避免正常并发被识别为重放。

小程序本地仅持久化必要的本系统凭证，不缓存证明照片或全量学生名单；账号切换清理列表、草稿和身份缓存。退出、封禁、密码重置、换绑按规则撤销会话。

### 7.4 自动业务身份与历史只读

有效用户绑定有效学生后自动维护 STUDENT；解除绑定、学生失效或账号停用后对应能力失效。志愿者资格按学期导入、启停并自动维护 VOLUNTEER；仅有角色而没有当前学期有效资格不得执行提交。先导入资格后绑定的路径也应补齐身份。

志愿者当前分配关系提供执行权，历史参与关系提供本人历史只读权。改派、资格停用或学期结束后仍可读当时任务/排班、本人提交、异常明细、审核意见和本人照片；不得读取新执行信息、继续操作或读取当前完整名单。照片仍受到期清理规则限制，账号有效和有效会话仍是所有读取的前提。

## 8. 数据库公共规范

1. 业务主键 BIGINT AUTO_INCREMENT；API 中 ID 统一以字符串传输，避免 JavaScript 整数精度问题。
2. utf8mb4、InnoDB；业务时间数据库统一使用 UTC 的 DATETIME(3)，API 返回带时区的 ISO 8601，界面按 Asia/Shanghai 展示。
3. 学期日期、查课日期用 DATE 表示本地日历日期；截止时间转换为明确的 UTC 时刻保存。
4. 状态使用 VARCHAR + Python StrEnum，并使用必要 CHECK 约束；保存稳定字符串值，不保存枚举序号。
5. 关联关系使用外键和明确删除策略；重要业务记录禁止级联物理删除。
6. 可修改记录使用 lock_version 或业务 current_version 做并发校验。
7. created_at、updated_at 不替代历史版本或审计。
8. 各业务查询建立联合索引，禁止机械地给所有字段加索引。

## 9. 核心数据模型

下表为逻辑模型，实施时补充长度、非空、外键、索引和迁移 SQL。除关联表外默认带 id、created_at、updated_at；不可变版本表只需要创建时间。

### 9.1 身份与基础数据

| 表 | 核心字段与约束 |
|---|---|
| user_account | username 唯一可空、password_hash、student_id 唯一可空、display_name、status |
| wechat_identity | appid、openid、user_id；UNIQUE(appid, openid) |
| auth_session | user_id、client_type、refresh_token_hash、refresh_family_id、expires_at、revoked_at |
| identity_binding_token | student_id、token_hash、expires_at、used_at、failed_attempts |
| role / permission | code 唯一、name |
| user_role / role_permission | 关联双方组成联合主键 |
| user_permission | user_id、permission_id 联合唯一、granted_by、granted_at |
| semester | code 唯一、name、start_date、end_date、first_monday、status |
| period_definition | semester_id、period_no、start_time、end_time；联合唯一 |
| administrative_class | class_code 唯一、class_name、grade_year、major_name、status |
| student | student_no 唯一、name、administrative_class_id、status |
| course | course_code、course_name、status |
| teaching_class | semester_id、course_id、class_code、class_name、status |
| teaching_class_student | teaching_class_id、student_id 联合主键 |
| course_schedule | semester_id、teaching_class_id、weekday、start_period、end_period、classroom、status |
| course_schedule_week | schedule_id、week_no 联合主键 |
| calendar_override | semester_id、date、override_type、source_teaching_date、reason；处理停课、调休与补课 |
| volunteer_qualification | semester_id、student_id 联合唯一、enabled、created_by |

student 的年级从行政班取得，避免重复维护而漂移。历史年级使用任务名单快照。志愿者资格依学生按学期维护，未绑定账号可以导入资格，分配任务前必须已有有效账号。

身份解析需提供可信 student_id、client_type、PRE_BINDING/正常状态及有效角色权限。PRE_BINDING 作为微信会话受限状态，具体持久化或实时推导在身份实现时统一约定；不增加 RoleCode。user_role 中的学生/志愿者成员关系随绑定与资格维护，不能作为独立于业务事实的手工开关。

### 9.2 任务、名单、截止时间与排班

| 表 | 核心字段与约束 |
|---|---|
| inspection_task | task_key 唯一、semester_id、inspection_date、week_no、inspection_type、起止节次、目标引用、课程/班级/教室快照、require_photo_snapshot、expected_count_snapshot、expected_count_current、roster_version、canceled_at/by/reason、lock_version |
| task_roster_version | task_id、version_no 联合唯一、reason、created_by |
| task_roster_member | task_id、roster_version、student_id 联合唯一、student_no/name/class/grade 快照 |
| submission_deadline_day | semester_id、inspection_date 联合唯一、deadline_at、version、updated_by、reason |
| submission_deadline_version | deadline_day_id、version_no 联合唯一、deadline_at、changed_by、reason |
| inspection_assignment | task_id 唯一、volunteer_user_id、assign_method、assign_reason、assigned_by、lock_version |
| assignment_change_request | assignment_id、request_user_id、reason、status、processed_by/at/comment |
| volunteer_day_lock | volunteer_user_id、inspection_date 联合主键；并发排班串行化锚点 |

任务唯一键包含学期、实际日期、类型、完整节次范围和目标标识。课程任务目标使用 course_schedule_id 等稳定来源身份，避免同名课程冲突；调课生成新的实际日期任务，旧任务取消并审计。

早晚自习必须有明确目标行政班及时间定义，课程任务必须有教学班和课程来源。名单和应到人数分别保存，人数人工调整不隐式增删名单。

### 9.3 提交、考勤与异议

| 表 | 核心字段与约束 |
|---|---|
| inspection_submission | task_id、attempt_no 联合唯一、volunteer_user_id、roster_version、result、review_status、submitted_at、deadline_version_id、late_at_submission、note、reviewed_by/at/comment |
| submission_abnormal_item | submission_id、student_id 联合唯一、attendance_type、note |
| submission_file | submission_id、file_id 联合主键 |
| attendance_record | task_id、student_id 联合唯一、effective_type、current_version、source_submission_item_id |
| attendance_record_version | attendance_record_id、version_no 联合唯一、attendance_type、source_type/id、changed_by、reason |
| objection | attendance_record_id、student_id、base_attendance_version、reason、desired_type、initial_status/by/at/comment、final_status/by/at/comment、final_attendance_type |
| objection_file | objection_id、file_id 联合主键 |

提交只录入异常项；审核通过后为该名单版本的每名学生建立初始考勤，未列异常者为 NORMAL。这样“全部正常”和“未提交”明确区分，学生可以查看正常记录，后续管理员也可更正原先漏报的异常。后续更正修改当前认定并追加版本，不重写原提交。

同一任务只能有一个待审核提交或一个审核通过提交。不同 attempt 的唯一约束不足以保证此规则，必须配合任务行锁及状态检查。审核通过后 V1.0 不接受重新提交，错误通过考勤更正处理。

### 9.4 文件、周报、导入批次与审计

| 表 | 核心字段与约束 |
|---|---|
| file_object | object_key 唯一、category、original_name、content_type、size_bytes、sha256、uploader_user_id、status、retention_policy_version、expires_at、purged_at |
| file_retention_policy | category、version、duration、anchor_type、effective_at、changed_by |
| report | semester_id、week_no、scope_key 联合唯一、latest_version_id、latest_updated_at |
| report_version | report_id、version_no 联合唯一、source_revision、rule_version、template_version、snapshot_file_id、excel_file_id、generated_at/by、reason |
| report_source_revision | semester_id、week_no 联合主键、revision；相关源数据变更时同事务递增 |
| import_batch | type、file_id、status、preview_digest、total_rows、success_rows、failed_rows、error_file_id、created_by |
| idempotency_request | user_id、operation、idempotency_key 联合唯一、request_hash、status、attempt_token、started_at、result_ref、expires_at；跨文件生成阶段安全重试时校验执行令牌 |
| audit_log | actor_id、action、resource_type/id、before_json、after_json、reason、request_id、created_at |
| system_config | key 唯一、value_json、version、updated_by |

report_source_revision 用于发现周报是否落后于源数据，不是业务冻结状态。报表文件具有自己的归档期限，不能套用临时文件或查课照片清理规则。

## 10. 历史事实与基础数据变更

任务生成时保存课程、教室、起止时间、名单及学生当时行政班/年级。历史统计按快照分组，不随当前学生转班自动改变。

执行前名单确实有误时，管理员创建新的名单版本并记录原因；保留初始名单及人数快照。提交事务锁任务并绑定当前名单版本，创建有效提交后禁止直接修改该任务名单。

后续名单、课表、节次时间变化影响新任务。历史任务的起止时刻使用自身快照，避免修改节次定义后历史逾期和任务时间漂移。

## 11. 任务生成与自动排班

### 11.1 任务生成

按学期、周次或日期、教学班/行政班、查课类型选择范围。先预览再生成；重复生成通过 task_key 返回既有任务或跳过，不报模糊的服务器错误。

停课日不生成课程任务，调休日按日历覆盖规则使用对应教学日课表。任务生成、名单快照和审计在同一事务内完成；超出单次规模上限时在写入前拒绝并提示缩小选择范围，V1.0 每次请求全成全败。

### 11.2 自动排班

硬约束：无本人课程冲突、无其他查课任务时间冲突、避开包含本人行政班学生的被查范围、账号有效、当学期资格有效。

对跨行政班教学班，只要目标名单中包含志愿者所属行政班学生，就按本班回避规则排除。课程及任务冲突按真实起止时间区间判断，不仅比较起始节次。

软约束：尽量均衡当前学期已分配次数；相同条件采用稳定顺序或固定种子，输出可解释理由。优先安排可用志愿者较少的任务，无可用人选则保留未分配并说明原因。

算法先计算候选方案，提交时短事务加锁并重新验证。手工调整也走相同约束，不允许前端绕过。已待审核或已完成任务禁止普通换人操作。

## 12. 查课提交、审核与任务状态

志愿者只能处理当前分配给本人的有效任务。输入 NORMAL 时异常项必须为零；ABNORMAL 时至少一项。异常类型为 LEAVE、LATE、ABSENT，学生必须属于提交所用名单版本，同一学生不能重复录入。

执行 submission.create 同时要求 VOLUNTEER、当前学期有效资格、当前受派关系和允许提交的状态。本人历史读取走 OWN_SUBMISSION，不把历史读取资格当作当前操作资格。任务中的排班基本信息与 inspection.read 一起授权，不单独定义 assignment.read。

提交前校验照片要求、附件归属、名单和任务状态；保存提交、异常明细、附件关系及审计在同一事务完成。被驳回提交不覆盖，重新提交生成下一 attempt。

任务状态按以下优先级推导：

```text
有取消标记 → 已取消
有审核通过提交 → 已完成
有待审核提交 → 待审核
其他情况 → 待执行
```

列表状态通过集合查询/聚合获取，禁止对每条任务逐次查询数据库。审核通过时锁任务及提交，检查仍待审核，然后生成考勤当前记录、初始版本和审计。审核驳回保留原事实和照片。

## 13. 本日提交截止时间与未完成统计

### 13.1 配置与生效

提供每日默认时间和指定日期覆盖。首次为某天创建任务时生成该日截止记录，不能只在统计时读取一个会变动的全局默认值。

修改全局默认值只影响后续新建日期记录；修改某日截止时间走专用接口，记录版本和原因。后台显示日期、原值、新值、影响任务数。修改后通知刷新中的页面，但服务端判断始终以事务中的有效版本为准。

截止时间不早于当天最晚相关任务结束时刻。V1.0 日内任务为主，跨午夜任务需单独定义业务日后才能支持。

### 13.2 当前未完成与截止时未完成

当前未完成：当前服务端时间超过该日截止时间，任务未取消，且没有待审核或审核通过提交。待审核不计为未完成。

截止时未完成：以某个截止时间版本对应的时点回看提交及审核历史。迟交不改变已经发生的截止时事实。报表同时区分“当前未完成”和“截止时未完成”，不得混用同一指标名称。

若截止后驳回原提交：当前任务回到待执行，可出现在当前未完成列表；原提交在截止时若仍待审核，不应倒算为当时未提交。需要使用 submitted_at、reviewed_at 和历史 attempt 计算。

### 13.3 补交与逾期

本版推荐允许补交。每次提交记录截止时间版本和 late_at_submission，不覆盖原提交时间。截止后改时间不偷偷重写既有逾期标记，页面与导出可解释适用的截止版本。

逾期不新增第五种任务状态。审核退回后的重提也记录自己的逾期事实，同时保留首次提交时间；考核是否豁免退回重提属于业务制度，V1.0 不自动作处罚判断。

## 14. 应到人数、考勤更正与异议

应到人数保留初始快照及当前值。修改须填写原因，使用版本校验，并满足非负、异常人数不超过当前应到人数等约束。人数变更不删除任何学生历史认定。

学生只能针对本人记录提交异议，记录发起时考勤版本。锁定考勤记录后检查同一记录是否已有未完成异议，保证并发下只创建一个。

初核状态为 PENDING、PASSED、REJECTED；终审状态为 PENDING、APPROVED、REJECTED。初核意见为参考，不直接改考勤；初核拒绝也由教师终审形成最终结论。无负责人获初核权限时，教师可直接终审并记录跳过初核原因。

负责人取得 objection.initial_review 后，可读取全学院全部异议、历史、关联考勤及初核所需证明；不是只可读待初核队列。关闭该权限后，attendance.read 不提供替代的证明读取权。

终审通过且需要更正时，在同一事务更新异议、考勤当前值、考勤版本、审计和报表源修订号。若考勤版本已与发起异议时不同，返回业务冲突，管理员重新查看后确认，不覆盖他人的新认定。

## 15. 并发、一致性与幂等

| 操作 | 必须使用的控制 |
|---|---|
| 重复生成任务 | task_key 唯一约束 + 返回既有结果 |
| 绑定码核销 | 条件更新 unused + 唯一学生绑定 + 同事务 |
| 查课提交 | 用户/操作/幂等键唯一 + task 行锁 + attempt 唯一 |
| 审核与取消竞争 | 两者锁同一任务并重验状态；审核状态条件更新 |
| 考勤更正 | current_version 条件更新，成功后追加相应版本 |
| 异议创建与终审 | 锁考勤及异议，校验未完成异议和旧版本 |
| 自动/人工排班 | 锁志愿者日期锚点，重查区间冲突，再更新分配 |
| 周报版本生成 | 锁 report，分配唯一版本号；幂等发布已完成文件 |
| 到期材料清理 | 防重叠执行 + 文件状态条件更新 + 可重复执行；失败留待下次运行 |

仅对 assignment 表查找重叠记录并加锁不够：无记录时可能没有稳定锁对象。volunteer_day_lock 为固定锁锚点，先安全建立或取得记录，再锁定并检查。转移任务时同时锁原、新志愿者日期锚点。

定义全局锁层级及同层主键升序：志愿者日期锚点 → 任务 → 提交 → 考勤 → 异议 → 报表源修订记录。没有涉及某层时跳过；报表发布使用独立短事务。发生死锁或锁超时，回滚并对可安全重试的完整操作做有限重试。

客户端写请求携带 Idempotency-Key。同一键相同请求返回原资源；同一键不同请求体返回冲突；运行中的同一请求返回可重查的状态。幂等记录与业务事务协调，不能先标记成功后实际写失败。

数据库唯一约束是最终防线；按钮置灰只是交互。threading.Lock、asyncio.Lock 和 Python GIL 都不能代替跨进程、跨实例的数据库并发约束。

## 16. 文件上传、访问与保留

### 16.1 上传与关联

V1.0 先采用后端接收受限文件并写对象存储，减少直传协议复杂度。后续若带宽成为瓶颈，可改为短时受限直传凭证 + 服务端完成核验，业务权限和文件模型保持不变。

默认允许 jpg、jpeg、png、webp；建议单文件不超过 10 MB、单次不超过 5 个，均为可配置上线参数。限制压缩前后大小、图像像素和解码资源；校验扩展名、声明 MIME、真实文件签名及解码结果。对象键由服务端随机生成，不使用上传原文件名作为路径。

file_object 经 UPLOADING → READY → PURGE_PENDING → PURGED 等状态管理，失败可记 FAILED。仅 READY 文件可关联业务。关联时检查类别、上传者、目标资源权限和是否已过期。

### 16.2 访问

GET /files/{id}/access 校验身份、功能及业务资源归属后返回短时签名链接。存储桶关闭公共读取，长期密钥不进前端。签名有效期由服务端配置，示例 5 分钟。

不实现每次实际下载重新鉴权，不即时撤销已发出的链接；链接在到期前可能继续使用。日志不记录完整签名 URL，禁止通过匿名 CDN 暴露私有附件。

### 16.3 保留与删除

按材料类别分别设置期限、起算事件和策略版本，具体数值待业务确认。计算出的 expires_at 保存到文件，不能因配置改变就无审计地缩短旧材料期限。

与未完成异议相关的材料暂停清理；历史提交的证明照片是否被异议引用须有可查询关联。延长期限可批量执行，缩短既有材料期限须预览影响范围并由有权限管理员显式执行。

清理先标记待清理，删除对象存储文件及适用历史对象版本，成功后记录 purged_at；失败重试，不将失败文件标记为已清理。访问已清理文件返回明确的 FILE_EXPIRED。备份按自身生命周期淘汰，到期清理不会保证所有既有备份即时消失。

## 17. 统计口径

仅统计审核通过且未取消任务的当前有效考勤，不把待审核异常计入正式缺勤。

```text
有效应到人数 = 当前有效应到人数 - 请假人数
缺勤率 = (迟到人数 + 旷课人数) / 有效应到人数
到课率 = 1 - 缺勤率
```

有效应到人数小于等于零，返回 null 和 statistics_available=false，不显示 0% 或 100%。聚合统计先累计分子分母再相除，不对每班百分比简单平均。

教学班可能跨行政班。班级统计按任务名单快照分配学生及异常人数；若人工只调整了教学班总应到人数却未提供班级分配，该任务班级分母不可可靠推导。接口应要求同时填写分班人数调整，或明确标记该任务不能用于班级比例统计，不能随意均摊。

“查课次数”按独立任务计算；重提 attempt 不增加次数。“未完成”与“待审核”分别列示。志愿者工作量同时区分已分配和已审核完成，避免算法与绩效口径混用。

## 18. 周报版本与最新版本时间

### 18.1 版本语义

周报按学期、周次和统计范围建立主记录。每次正式生成新版本保留统计明细快照与 Excel。旧版本不可编辑覆盖；重复下载同一版本不新增版本。

report.latest_updated_at 表示最新成功版本的生成时间，界面显示“最新版本更新时间”。如果界面使用“最终版本修改时间”，必须标明指当前最新版本，并非禁止以后修订。

### 18.2 一致性生成流程

1. 同步生成接口校验发起人、范围、原因、数据量上限和幂等键；登记生成请求状态，不创建通用后台作业。
2. 在一次一致性只读事务中读取源修订号、任务、名单、考勤、截止版本与汇总；使用同一数据库快照。
3. 结束读取事务后，根据已取得数据生成 JSON 快照和 Excel，写入私有对象存储。
4. 以短事务锁定 report，校验文件已就绪并创建不可变 report_version，更新 latest_version_id、latest_updated_at，写审计。
5. 全部完成后返回 201、版本 ID 与下载入口；失败不改变最新版本指针，未引用的临时对象按规则回收。响应丢失时客户端用原幂等键重试，返回已完成版本。

并发生成必须防止读取旧源数据的慢请求晚于较新版本完成而成为最新版本。发布时比较 source_revision；发现结果已过时则返回 409 提示重新生成，不更新最新版本。用户重新生成使用新幂等键，单纯网络重试仍使用原键。

### 18.3 源数据变化提示

影响该周报的考勤、应到人数、任务、截止时间等修改，同事务递增对应 report_source_revision。最新版本的源修订号落后时显示“源数据已更新，请生成新版本”。统计公式、模板更新也通过版本比较提示。

生成新版本不锁定业务、更正、异议或旧版下载。周报不承担业务发布审批，学院通过原有渠道发布文件。

## 19. Excel 导入与导出

导入流程：上传 → 受限解析 → 字段与关系校验 → 预览和错误报告 → 确认 → 重新验证关键引用 → 事务写入 → 审计。

预览保存内容摘要，确认时不得替换文件；检测预览后基础数据变化，必要时要求重新预览。V1.0 每个确认批次全成全败；超过行数或文件规模上限时拒绝并提示拆分文件，不提供请求内部分成功或后台自动续跑。

限制行数、列数、文件大小、压缩解包比和处理时长，拒绝宏文件及非许可格式，不执行外部链接或用户公式。导出用户文本使用明确的文本单元格，避免被解释为公式。

既有 .xlsx 模板使用 openpyxl.load_workbook 的普通可编辑模式填充，验证合并单元格、样式、打印区域和分页。大数据导入使用 read_only=True 并显式关闭工作簿；从头生成的大型明细表可用 Workbook(write_only=True) 逐行写入，不能把该模式当作编辑既有模板的替代。

openpyxl 不计算 Excel 公式。业务统计结果由后端计算并写为数值，不能依赖读取到的公式缓存作为最新统计。模板若包含公式，须验证实际 Excel/WPS 打开后的行为；含特殊绘图等对象的模板要做往返保存兼容性检查，不承诺原样保留所有 Excel 特性。

导入只接受经校验的 .xlsx，解析使用适当 XML 防护（例如 defusedxml）及 ZIP 资源限制。报表模板有版本和摘要，以学院真实样例验收；临时文件在成功及失败路径都清理，不能让并发请求共用同一个 Workbook 实例。

## 20. API 契约

前缀 /api/v1，所有身份依后端会话判断。以下为资源与操作基线，实施时输出完整 OpenAPI。

逐接口权限映射以 docs/PERMISSIONS.md 第 12 节为准，并叠加第 11 节复合权限及 PRE_BINDING 白名单。账号读写仅超管；角色分配可由教师在受限范围执行；绑定管理面向超管、教师与负责人。GET /import-templates/{type} 只需目标资源 manage，导入实际执行及批次读取需再具备 import.execute。

| 模块 | 主要接口 |
|---|---|
| 登录 | POST /auth/web/login、/auth/wechat/login、/auth/refresh、/auth/logout |
| 本人 | GET /me；POST /me/student-binding |
| 权限 | GET /users/{id}/optional-permissions；PUT /users/{id}/optional-permissions/{code} |
| 账号与角色 | GET/POST /users；GET/PATCH /users/{id}；PUT /users/{id}/roles；账号读写与角色分配分别授权 |
| 绑定管理 | POST /students/{id}/binding-tokens；POST /binding-tokens/{id}/revoke；POST /users/{id}/student-binding-reset |
| 基础数据 | /semesters、/students、/administrative-classes、/teaching-classes、/course-schedules |
| 导入 | POST /imports；GET /imports/{id}；POST /imports/{id}/confirm |
| 任务 | POST /inspection-tasks/generate；GET /inspection-tasks；GET /inspection-tasks/{id} |
| 排班 | POST /assignments/auto；PUT /inspection-tasks/{id}/assignment；POST /assignment-change-requests |
| 取消 | POST /inspection-tasks/{id}/cancel |
| 截止 | GET/PUT /submission-deadlines/default；GET/PUT /submission-deadlines/days/{date} |
| 志愿者 | GET /me/inspection-tasks；GET /inspection-tasks/{id}/students |
| 提交 | POST /inspection-tasks/{id}/submissions |
| 审核 | POST /submissions/{id}/review |
| 考勤 | GET /me/attendance；GET /attendance；POST /attendance/{id}/corrections |
| 异议 | POST /attendance/{id}/objections；POST /objections/{id}/initial-review、/final-review |
| 附件 | POST /files；GET /files/{id}/access |
| 统计 | GET /statistics/attendance、/statistics/incomplete-tasks |
| 周报 | POST /reports/weekly/versions；GET /reports/{id}/versions；GET /report-versions/{id}/download |

创建成功返回 201，同步查询/更新按接口定义返回 200 或 204；不提供异步 jobId 或作业轮询接口。未登录 401，无权 403，资源不存在 404，版本/状态冲突 409，数据校验失败 422，限流 429。必要时对不可见资源统一返回 404，避免泄露资源是否存在。

成功响应包含 data、requestId；分页包含 items、page、pageSize、total。错误包含 code、message、fieldErrors、requestId；不返回堆栈或数据库内部信息。

典型业务错误：STATE_CONFLICT、VERSION_CONFLICT、ASSIGNMENT_CONFLICT、DUPLICATE_ACTIVE_OBJECTION、FILE_EXPIRED、IDEMPOTENCY_KEY_REUSED。前端针对错误给出明确刷新、重试或修改提示。

## 21. 原生微信小程序实现

### 21.1 页面与文件

每页使用同名 .wxml、.js、.wxss、.json 文件。使用 Page、Component、App 及微信原生 API，不引入 Vue Router、Pinia 或基于 DOM 的组件。

```text
pages/
├── login/             微信登录
├── binding/           学生绑定
├── home/              根据身份展示入口
├── my-tasks/          本人查课任务
├── task-detail/       详情、截止时间及名单
├── submission/        正常/异常录入和照片
├── assignment-change/ 调班申请
├── my-attendance/     本人考勤
├── attendance-detail/ 认定与历史
├── objection-create/  异议材料提交
├── objection-detail/  处理进度
└── profile/           身份、退出、说明
```

services/request.js 封装 wx.request，统一基础 URL、超时、认证、串行刷新和错误转换。上传、签名链接下载分别封装；下载后通过适用的原生预览能力打开。

纯 JavaScript 常量、数据转换和 JSDoc 类型用于维护契约，可从 OpenAPI 生成或校验接口描述，但不要求小程序使用 TypeScript。

### 21.2 交互与可靠性

搜索仅请求本人任务名单，输入防抖、分页；setData 只更新必要字段，不向页面一次性注入全院名单。异常学生唯一选择，姓名同名时显示学号和行政班。

页面显示提交状态与服务器确认结果。网络超时不能提示“提交失败请重新新建”，应使用同一幂等键重查或重试。仅确认成功后清理草稿；草稿按用户与任务隔离且设置清理期限。

照片先完成上传再提交业务引用；上传失败可重试单张，不重复上传全部。未完成上传不能绕过必需照片校验。客户端压缩改善体验，服务端仍执行全部校验。

onShow 重新获取必要状态，处理管理员改派、取消和截止时间修改。401 尝试一次受控刷新，失败回登录；409 显示冲突并重新加载，不静默覆盖。

### 21.3 发布准备

分别配置开发、体验和生产 API 地址；AppID 与 AppSecret 分离。按微信平台要求配置 HTTPS 请求、上传、下载域名，准备隐私说明和必要的用户授权交互。

微信开发者工具模拟器不能替代真机测试。至少覆盖 iOS 和 Android，验证登录、图片选择、弱网、后台切换、登录失效、下载预览与新版本升级。

## 22. Web 管理端实现

包含工作台、基础数据、课表、任务、排班、审核、考勤、异议、统计、周报版本、学生工作负责人权限、每日截止时间及系统配置。

Pinia 仅保存当前用户、权限、当前学期和少量 UI 状态，列表数据按接口查询。路由和按钮控制只作展示辅助，后端独立校验。

周报列表显示版本号、生成时间、操作人、生成原因、源数据是否已变化，支持下载指定历史版本。截止时间修改显示具体影响日期与数量。可选权限按人员逐项开关，页面保存失败时恢复原值。

## 23. 安全与审计实施要求

1. MySQL 仅私网或容器网络可达，不开放公网数据库端口；应用账号不使用 root，迁移账号与运行账号分离。
2. 配置与存储密钥通过受保护环境配置注入，不进入仓库或前端包；日志过滤密码、令牌、绑定码、签名 URL 和证明原文。
3. SQL 全部参数绑定，排序字段使用许可清单；前端避免直接渲染不可信 HTML，文件不在应用可执行目录落地。
4. 审计覆盖提交、审核、取消、调班、人数/名单更正、异议处理、权限修改、身份换绑、截止调整、报表生成下载与材料清理。
5. 关键业务变更与审计在同一数据库事务；审计失败则变更回滚。下载链接签发记录访问事件，实际对象下载不逐次重鉴权。
6. 审计表对应用账号允许 INSERT 和必要 SELECT，不允许 UPDATE/DELETE；运维账号单独管理，不能宣称数据库管理员无法篡改。
7. 审计只存必要差异，避免把证明全文复制到长期日志。开发测试使用合成或脱敏数据。
8. 用户资料、照片、考勤、报表与日志分别形成保留策略，明确访问人员及清理责任。正式保留期由学院制度确定。

## 24. 同步执行、定时清理与失败处理

### 24.1 有界同步操作

Excel 导入、自动排班与周报生成在请求内完成；文件大小、行数、日期范围、任务数量与同时执行数量均设可配置上限。超过上限在业务写入前返回明确错误，提示拆分文件或缩小范围。

先解析/计算，再以短事务重验并提交；生成 Excel 与上传对象不持有业务数据库事务。普通列表与提交接口不应被大量导出挤占，昂贵操作设置按用户和全局的有限并发；多进程下不能仅用单进程信号量表达全局限额，可由共享入口限流和数据库幂等记录配合实现。

按允许的最大输入量测量耗时，协调服务端操作预算、SQL/对象存储超时及 Nginx/客户端等待时间。客户端断开不保证同步线程立即停止，因此超时不代表写入没有发生，不能直接换新幂等键重做。

### 24.2 重试与中断

保留 import_batch、report_version 和 idempotency_request 业务记录，不建立 background_job。相同请求重试使用原幂等键：已完成则返回原结果，仍执行中则返回 409 OPERATION_IN_PROGRESS 并提示稍后重试；不返回通用 jobId，不自动无界重试。

未提交数据库事务在进程终止后回滚；已提交但响应丢失的操作通过幂等结果查回。周报在开始登记幂等请求、结束发布版本之间存在无事务文件生成阶段：中断可能留下处理中记录。达到规定的最大运行预算后，可通过带 attempt_token 的条件更新接管原键重试；旧执行路径完成时必须校验同一 token 才能发布，避免旧请求迟到造成重复版本。此机制仅用于请求去重与安全重试，不提供自动后台续跑。

幂等记录的保留期限覆盖客户端允许的重试窗口；业务唯一约束继续作为最终防线。错误日志记录请求 ID，业务失败不能伪装成成功。

### 24.3 到期材料清理

deploy/scripts/cleanup_expired_files.py 由部署环境每日定时执行，复用文件服务、配置与审计，独立创建 Session。指定唯一调度入口并防止两次脚本重叠；多实例部署时可用数据库命名锁等全局互斥方式，不依赖每个 Web 进程各自启动定时器。

分批查询到期候选，事务内重验保留条件并标记 PURGE_PENDING，再在事务外删除对象，最后写清理结果。业务建立新的材料引用时须锁定并核验文件仍为 READY；与清理按相同文件锁协议串行化，避免校验后新增异议关联而误删。

删除失败保留可重试状态，下次调度继续处理；对象已删除但结果未登记时，重复删除按对象已不存在视作成功。脚本支持预览模式、批量上限、运行摘要与失败退出码，由部署监控报告失败。未关联对象须超过明确清理宽限期，并排除仍在执行的生成请求文件。

### 24.4 后续扩展条件

若真实数据下操作经常需要十几秒以上、逼近请求超时，或明确要求关闭页面后仍可靠执行，再评估持久化任务表与独立执行进程。该条件是评估触发点，不是当前已确认的性能阈值或开发范围。

不使用 FastAPI BackgroundTasks 代替重要业务的可靠执行；V1.0 不承诺请求中断后自动续跑。同步模式也不免除限流、资源预算和数据库并发控制。

## 25. 容量规划与性能验收

### 25.1 负载假设

尚未取得实际人数和服务器规格，以下仅为首轮压测样例：5,000 学生、300 志愿者、30 管理用户，准备多个学期累计数据。实际压测数据必须覆盖预计保留期规模。

场景包含登录绑定高峰、学生集中查考勤、课后集中提交及上传、管理审核与导出同时运行。不能用总注册人数直接等同并发请求数。

### 25.2 首轮建议目标

| 项目 | 待验证目标 |
|---|---|
| 常规查询与小事务 API | 50 RPS 混合负载持续 30 分钟 |
| 突发 | 150 RPS 持续 1 分钟，观察限流和恢复 |
| 延迟 | 常规 API P95 ≤ 1 秒；同步导入、排班、报表及文件传输单独统计，并验证允许规模内的耗时低于请求预算 |
| 服务错误 | 预期业务冲突及主动限流单列；非预期 5xx < 0.1% |
| 数据正确性 | 无重复生效提交、重复终审、丢失更正或重叠排班 |
| 恢复 | 请求中断或服务重启后，已提交结果可查询/重放，未提交操作可安全重试，不承诺自动续跑 |

目标通过真实数据库、代表性数据和规定硬件验证，不能作为当前已达成的指标。另测文件上传带宽、Excel 内存及学生真机弱网体验。

### 25.3 参数原则

每个 Uvicorn 服务进程都有自己的连接池，清理脚本也要预留独立连接预算。预算按所有进程的 (pool_size + max_overflow) 求和，再加迁移和维护预留，不得超过数据库可用连接数。设置有限 pool_timeout、适合 MySQL 空闲连接策略的 pool_recycle 和 pool_pre_ping，监控连接等待及超时。

同步路由线程池、数据库连接池和请求限流共同限定并发；线程数、进程数或协程数不是可承载请求量承诺。先从少量 Uvicorn 进程起步，按 CPU、内存及数据库等待压测调整。

关键索引覆盖任务日期/学期、志愿者任务、学生考勤、提交状态、异议待处理、到期文件和周报版本查询。通过执行计划和慢查询验证，分页限制最大 pageSize，深分页按需求使用游标。

## 26. 部署、备份与监控

生产组成：nginx、backend、mysql（或托管 MySQL）；附件存 COS/OSS。只有 HTTPS 入口对公网开放，管理端静态资源同源提供。

可将 4 核 8 GB 作为应用与数据库同机的首轮压测起点；预算较低可从更小规格试运行。配置选择均须基于测量，不承诺固定规格承载能力。为各 Python API 进程、清理脚本、MySQL、文件解析和系统分别预留内存并限制容器资源。

backend 使用 Uvicorn 启动 app.main:app，生产关闭 reload；材料清理由部署环境定时执行 deploy/scripts/cleanup_expired_files.py，可复用应用镜像作为一次性命令运行。开发示例为 uv run uvicorn app.main:app --reload；生产示例为 uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --workers 2，进程数 2 仅为待压测起点，端口只在内部网络供 Nginx 访问。以上命令须在工程骨架建成后运行。

单独发布步骤使用 uv run alembic upgrade head；只通过受保护环境配置提供 DATABASE_URL、令牌密钥、微信 AppSecret 与对象存储凭证。存活检查验证进程，准备就绪检查验证数据库连接和迁移版本，不能仅进程存在就放量。

上线按构建版本、数据库迁移、配置校验、健康检查、关键链路冒烟执行。数据库变更优先向后兼容；应用回滚不能假设数据库迁移天然可逆。

备份包括数据库、对象、报表快照、模板、配置恢复资料。异机加密保存，每日全量备份，是否启用 binlog 时间点恢复由可接受数据损失决定。

建议讨论目标：RPO ≤ 1 小时、RTO ≤ 4 小时；若采用此目标，仅每日备份不够，必须配置相应增量/binlog 归档并演练。该目标尚未通过验证。

上线前执行一次完整恢复演练，运行期定期演练；同时检查数据库引用与对象文件一致性。监控 CPU、Python 进程内存、请求延迟、线程池压力、磁盘、连接池等待、慢 SQL、锁等待、HTTP 错误、同步操作超时、清理脚本失败、上传失败和备份失败，配置可触达告警。

## 27. 测试与验收清单

工程检查包括 Ruff、mypy、pytest 和 Alembic 迁移验证。接口测试用 TestClient/HTTPX；约束、锁、事务与并发测试必须连接隔离的 MySQL 8.4 实例，不能用 SQLite 通过来证明 MySQL 正确。并发测试采用独立连接/Session 和受控同步点，明确构造竞争而不是依赖随机 sleep。

验证 Session 异常后释放连接、提交错误返回正确、请求中断后的安全重试和多进程幂等。Excel 验收包含 openpyxl 重新读取内容与学院模板的人工视觉比对。测试数据库与生产凭证完全分离。

### 27.1 业务与权限

- 五类角色、多角色重叠、学生工作负责人全年级访问。
- 38 个 Permission 与默认矩阵一致；教师 role.assign 仅可手工分配负责人，不得授予/撤销 SUPER_ADMIN 或 TEACHER_ADMIN（含本人）；超管可管理超管及教师角色。
- PRE_BINDING 仅四条会话/绑定路径；绑定后自动 STUDENT，资格导入自动维护 VOLUNTEER，资格停用阻止提交。
- 负责人可管理绑定、可读完整截止历史，有初核权限时可读全院异议历史，但不能终审或更正。
- 改派、资格停用、学期结束后仍可读本人历史照片（未清理时），不可操作原任务或读取改派后新内容。
- 可选权限默认无、逐人开启关闭、后续请求生效；不可自行授予终审权限。
- 学生修改资源 ID 无法获取他人记录；志愿者无法访问未分配任务和全院名单。
- 附件 ID 替换、跨用户引用、过期文件、短时签名访问均符合约定。
- 同名学生、跨班教学班、转班历史、停课补课和节次变更不改变历史事实。

### 27.2 并发与一致性

- 两次并发绑定只能一次成功。
- 同一请求反复重试只产生一个业务结果；不同内容复用幂等键被拒绝。
- 同一任务并发提交/审核/取消，只有符合状态的操作成功。
- 同一志愿者同时被不同任务分配，重叠任务不得同时成功。
- 人工更正与异议终审竞争不丢失版本；同一考勤并发创建异议只保留一个进行中结果。
- 审计写入失败时关键业务变更回滚。
- 对象写入失败、报表发布失败、请求中断或服务重启不改变既有最新有效版本；原幂等键重试不重复生成版本。

### 27.3 截止与报表

- 截止前无提交不计逾期未完成，截止后无提交计入；待审核单列。
- 逾期补交保留时间和截止版本，驳回重提不覆盖第一次提交。
- 截止时间修改有历史、权限和影响提示。
- 周报 V1/V2 文件与快照分别保留，下载 V1 不变成 V2。
- 考勤更正提示周报待更新；新版生成后记录最新时间；较旧并发请求不覆盖最新结果。
- 全勤、请假抵扣、零分母、跨班统计及总体加权结果正确。

### 27.4 真机与运维

- iOS/Android 登录绑定、拍照上传、断网重试、切后台恢复、退出换号、文件预览。
- 受限文件格式与大文件拒绝、材料到期清理、异议保留阻止误删。
- 规定负载下性能报告、备份恢复报告和上线回滚记录可复核。

## 28. 开发阶段与交付物

| 阶段 | 范围 | 阶段交付 |
|---|---|---|
| 0 | 业务参数与契约冻结 | 权限矩阵、截止规则、保留策略、模板、数据库设计、OpenAPI |
| 1 | 工程与安全骨架 | FastAPI/Web/原生小程序工程、SQLAlchemy 模型、Alembic 迁移、登录、绑定、权限与审计 |
| 2 | 基础数据 | 学期、学生、教学班、课表、校历、导入预览与确认 |
| 3 | 任务与排班 | 快照、幂等生成、冲突校验、手工调整、截止配置 |
| 4 | 查课闭环 | 原生小程序录入、照片、重试、管理审核、考勤版本 |
| 5 | 学生服务 | 本人查询、异议初核/终审、并发更正 |
| 6 | 统计与周报 | 统计口径、有界同步生成、不可变周报版本及最新时间 |
| 7 | 上线验收 | 越权/并发/真机测试、压测、材料清理、备份恢复与运维手册 |

优先尽早打通一条真机端到端链路：微信登录 → 绑定 → 本人任务 → 照片上传 → 提交 → Web 审核 → 学生查询。不得到最后阶段才验证微信登录和文件域名。

## 29. 开发前待冻结清单

1. 学生、志愿者、教学班名单、课表及周报的真实模板。
2. 学期第一周定义、节次时间、早晚自习时段与调休处理。
3. 日默认截止时间、逾期补交规则、修改截止时间的适用范围。
4. 查课照片、异议材料、临时文件、报表、审计及备份各自保留期限。
5. 初核跳过等流程规则；三个可选权限、负责人全院异议历史阅读已按权限基线确定。教师角色仅由超管授予/撤销，教师互授或撤销教师角色不再是待定项。
6. 绑定码发放渠道、换绑线下核验细节、管理账号来源；可执行绑定管理的三类角色、学生/志愿者自动身份和 PRE_BINDING 白名单已确定。
7. 名单修订与应到人数分班调整规则。
8. 实际用户量、提交高峰、累计学期数、部署预算与恢复目标。
9. 微信主体、AppID、服务器域名、隐私说明及发布负责人。
10. 精确依赖版本、兼容性验证、漏洞修复与升级责任人。

这些待定项不妨碍工程骨架和独立模块准备；涉及相应业务行为和自动清理时，必须先落实明确配置与规则。

## 30. 官方技术参考

以下用于选型和实现核对，不替代项目业务规则；依赖版本以开工时锁定的兼容版本为准。

- [FastAPI 同步与异步执行](https://fastapi.tiangolo.com/async/)
- [FastAPI 后台任务边界](https://fastapi.tiangolo.com/tutorial/background-tasks/)
- [SQLAlchemy Session 与并发模型](https://docs.sqlalchemy.org/en/20/orm/session_basics.html)
- [SQLAlchemy 事务管理](https://docs.sqlalchemy.org/en/20/orm/session_transaction.html)
- [Alembic 自动迁移与人工审核](https://alembic.sqlalchemy.org/en/latest/autogenerate.html)
- 微信小程序框架：https://developers.weixin.qq.com/miniprogram/dev/framework/
- 微信小程序 WXML：https://developers.weixin.qq.com/miniprogram/dev/reference/wxml/
- 微信小程序登录：https://developers.weixin.qq.com/miniprogram/dev/framework/open-ability/login.html
- [openpyxl 模板读写与限制](https://openpyxl.readthedocs.io/en/stable/tutorial.html)
- [openpyxl 只读与只写模式](https://openpyxl.readthedocs.io/en/stable/optimized.html)
- MySQL InnoDB 锁行为：https://dev.mysql.com/doc/refman/8.4/en/innodb-locks-set.html
- OWASP 授权设计：https://cheatsheetseries.owasp.org/cheatsheets/Authorization_Cheat_Sheet.html
- OWASP 文件上传：https://cheatsheetseries.owasp.org/cheatsheets/File_Upload_Cheat_Sheet.html

本文为设计产物，不代表系统已经实现，也不代表安全测试、真机验收或容量验证已经通过。
